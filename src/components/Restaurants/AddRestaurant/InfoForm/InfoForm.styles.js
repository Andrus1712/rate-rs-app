import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 10,
    marginTop: 5
  },
  textArea: {
    // height: 100,
    // width: "100%",
    // padding: 0,
    // margin: 0,
    // borderColor: "red",
    // borderWidth: 1,
  },
});
