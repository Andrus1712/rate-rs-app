import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  contentContainer: {
    backgroundColor: "#fff",
    flex: 1,
  },
  infoContainer: {
    borderWidth: 1,
    borderColor: "blue",
  },
});
